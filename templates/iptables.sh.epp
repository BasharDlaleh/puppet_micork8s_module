sudo iptables -t nat -I PREROUTING -i <%= $facts['networking']['primary'] %> -p TCP -d <%= $facts['ipaddress'] %>/32 --dport 80 -j DNAT --to-destination <%= $master_ip %>:80 || true &&
sudo iptables -t nat -I PREROUTING -i <%= $facts['networking']['primary'] %> -p TCP -d <%= $facts['ipaddress'] %>/32 --dport 443 -j DNAT --to-destination <%= $master_ip %>:443 || true &&
sudo mkdir /etc/iptables || true &&
sudo sh -c "iptables-save > /etc/iptables/rules.v4" || true &&
sudo sh -c "ip6tables-save > /etc/iptables/rules.v6" || true 
